import React from "react";
import './Footer.css';

const Footer = () => {
    return (
        <footer className="footer">
            <p>This will be the footer</p>
        </footer>
    );
};

export default Footer;